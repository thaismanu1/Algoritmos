#include <iostream>
#include <list>
using namespace std;

int main()
{
	list<int> pilha;
	int x;
	
	//inserindo dados na pilha
	for(int i = 0; i<4; i++)
	{
		cin >> x;
		pilha.push_front(x);
	}
	
	
	//removendo e mostrando elementos da pilha
	while(!pilha.empty())
	{
	cout << *pilha.begin() << endl;
	pilha.pop_front();
	}
	
	
	return 0;
}
